import { Routes } from '@angular/router';
import { ScreenPrincipal } from './components/screen-principal/screen-principal';

export const routes: Routes = [
  { path: '', component: ScreenPrincipal }
];